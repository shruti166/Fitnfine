const initiaState = {
    isAuthenticated: false,
    user: {}
}

export default function authReducer(state = initiaState, action) {
    switch(action.type) {
       default: 
       return state;
    }
}